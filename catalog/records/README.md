# Source records

Place one reviewed JSON record per file here. Use a stable directory partition once the first source
is approved. Every record must validate against `schema/catalog-record.schema.json`.
